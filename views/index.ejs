<!DOCTYPE html>
<html lang="ja">
<head>
  <meta charset="UTF-8">
  <title>ユーザー一覧</title>
  <style>
    body { font-family: sans-serif; margin: 20px; }
    ul { list-style-type: none; padding: 0; }
    li { background-color: #f0f0f0; margin-bottom: 5px; padding: 10px; border-radius: 5px; }
    form { margin-top: 20px; padding: 15px; border: 1px solid #ccc; border-radius: 8px; background-color: #f9f9f9; }
    input[type="text"] { padding: 8px; border: 1px solid #ddd; border-radius: 4px; width: 200px; margin-right: 10px; }
    button { padding: 8px 15px; background-color: #007bff; color: white; border: none; border-radius: 4px; cursor: pointer; }
    button:hover { background-color: #0056b3; }
  </style>
</head>
<body>
  <h1>ユーザー一覧</h1>
  <ul>
    <% users.forEach(user => { %>
      <li><%= user.name %></li>
    <% }) %>
  </ul>
  <h2>ユーザー追加</h2>
  <form action="/users" method="post">
    <input type="text" name="name" placeholder="ユーザー名を入力" required>
    <button type="submit">追加</button>
  </form>
</body>
</html>